{
  "system": {
    "backend_url": "http://localhost:8000",
    "update_interval_seconds": 5,
    "frame_skip": 2,
    "display_width": 800
  },
  "bus_stops": [],
  "notes": "Bus stops are now loaded dynamically from BODS API. Use the frontend app to find stop IDs, then add cameras here if needed."
}
